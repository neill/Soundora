'use strict';

describe('myApp.playview module', function() {

  beforeEach(module('myApp.playview'));

  describe('playview controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var playviewCtrl = $controller('PlayViewCtrl');
      expect(playviewCtrl).toBeDefined();
    }));

  });
});